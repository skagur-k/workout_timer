import 'package:flutter/material.dart';

class TimerDetailPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text("TIMER DETAILS PAGE"));
  }
}
